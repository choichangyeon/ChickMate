'use client';

const TTSComponent = () => {
  const handleClick = async () => {};
  return (
    <div>
      <button onClick={handleClick}>테스트</button>
    </div>
  );
};

export default TTSComponent;
